### IntelliJ IDEA ###
out/
!**/src/main/**/out/
!**/src/test/**/out/
.kotlin

### Eclipse ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache
bin/
!**/src/main/**/bin/
!**/src/test/**/bin/

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

### VS Code ###
.vscode/

### Mac OS ###
.DS_Store

### Python ###
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/

### PyInstaller build output (packaging/build.py, specs/020-cli-packaging) ###
/build/
/dist/

### Node.js (frontend/) ###
node_modules/
frontend/dist/

### LaTeX (docs/pfa.tex) ###
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc
docs/*.synctex.gz
docs/pfa.pdf

### This project's local runtime state ###
# Every component (repository_metadata, dependency_graph, vector_index,
# doc_generator's page manifest) persists to its own caller-supplied SQLite file -
# see docs/architecture.md "Storage architecture". None of these are meant to be
# committed, whatever path/name a caller gives them.
*.sqlite
*.sqlite3
*.db
# Default local runtime-state directory used when indexing a repository in place
# (see src/chat_api/server.py's --index-db/--metadata-db defaults).
# The `repo-scanner` CLI (019) does not use this in-repo location - its own
# state and config live under the user's home directory (~/.repo-scanner/),
# never inside an analyzed repository, so no separate ignore entry is needed
# for it here.
.repo-scanner/
# Local-only configuration/secrets (e.g. overriding the local LLM/embedding
# endpoint); nothing in this project reads a .env file today, but this project's
# local-only, no-cloud-credentials design means none should ever be committed if
# one is introduced.
.env
.env.*
*.log
